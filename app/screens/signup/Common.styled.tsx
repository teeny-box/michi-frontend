import { StyleSheet } from "react-native";

export const commonStyles = StyleSheet.create({
  container: {
    marginTop: 50,
    paddingHorizontal: 25,
    position: "relative",
    flexGrow: 1,
    backgroundColor: "#fff",
  },

  scrollBox: {
    paddingTop: 80,
    paddingBottom: 80,
  },
});
